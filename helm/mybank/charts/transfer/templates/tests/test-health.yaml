apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-transfer-test
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['{{ .Release.Name }}-transfer:8084/actuator/health']
  restartPolicy: Never
