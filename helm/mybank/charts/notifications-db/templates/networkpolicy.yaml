apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-notifications-db
spec:
  podSelector:
    matchLabels:
      app: notifications-db
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: notifications
      ports:
        - port: 5432
          protocol: TCP
