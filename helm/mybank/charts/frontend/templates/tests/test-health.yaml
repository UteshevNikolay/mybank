apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-frontend-test
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['{{ .Release.Name }}-frontend:8082/actuator/health']
  restartPolicy: Never
