apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-cash-db
spec:
  podSelector:
    matchLabels:
      app: cash-db
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: cash
      ports:
        - port: 5432
          protocol: TCP
