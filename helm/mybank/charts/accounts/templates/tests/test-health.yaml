apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-accounts-test
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['{{ .Release.Name }}-accounts:8081/actuator/health']
  restartPolicy: Never
