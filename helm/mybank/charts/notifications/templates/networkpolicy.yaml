apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-notifications
spec:
  podSelector:
    matchLabels:
      app: notifications
  policyTypes:
    - Ingress
  ingress: []
